import { FiltrotablePipe } from './filtrotable.pipe';

describe('FiltrotablePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrotablePipe();
    expect(pipe).toBeTruthy();
  });
});
