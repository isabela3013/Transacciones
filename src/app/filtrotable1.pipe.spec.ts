import { Filtrotable1Pipe } from './filtrotable1.pipe';

describe('Filtrotable1Pipe', () => {
  it('create an instance', () => {
    const pipe = new Filtrotable1Pipe();
    expect(pipe).toBeTruthy();
  });
});
